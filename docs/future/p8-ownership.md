# P8 field-level ownership map

Status: current authority/writer/reader columns are **IMPLEMENTED REALITY** at `ecbb3c5`; future authority and dispositions are a **PRODUCTION ARCHITECTURE DECISION**; every migration is **PLANNED ENGINEERING** after its replacement authority exists. v0.1.3 performs this audit, and A10.2/A10.3 enforce it. Broad P8 migration is **not** v0.1.3 work.

P8 works. The [implementation baseline](implementation-baseline.md#p8-authority-finding) records that no model output path writes P8 identity or relationship state, and that assistant-generated evidence is classified non-authoritative. P8 is therefore not rewritten to match the newer architecture. It becomes a compatibility/projection layer field by field, and it disappears only when replacement authorities actually exist.

## Map

“Reconstruction” means `reconstructP8MainProfile` / `reconstructP8Projection`, called by the Runtime orchestrator's `attachSemanticContext` on every turn and projected into Character ABI sections by `packages/character-abi/src/p8-projection.ts` for the A4 canonical context. Readers below are production readers. Tests are omitted.

| Field / group | Current authority | Current writer | Current readers | Future authority | Migration condition | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| `character.name` and identity invariants (`DEFAULT_AUTHORED_INVARIANTS`) | Authored code constant (`packages/p8/src/index.ts`) | Source change only | Reconstruction → canonical context IDENTITY | Governed authored charter/persona artifact, versioned | A versioned charter store exists with the same immutability and review rules | **Keep** as authored data; may **move** to a charter artifact without semantic change |
| Production persona statements (`persona.production.N`) | Authored code (`production-invariants.ts`) | Source change only | Reconstruction → PERSONA | Same governed charter/persona artifact | As above; style tests from [model replacement](model-replacement.md) pass | **Keep / move**; never learned from experience |
| Authored-invariant revision policy (`FIXED`/`USER_REVISABLE`) | Explicit overlay input; missing means `FIXED` | Source/config only | `applyP8Corrections` | Charter governance | With charter move | **Move** with charter |
| Explicit corrections (`P8ExplicitCorrection`: REVISE/RETRACT, lineage via `supersedesCorrectionReference`) | Append-only correction store: file adapter `p8-file-correction-store.ts` in the server; a PostgreSQL adapter exists in `p8-correction-store.ts` | `RuntimeOrchestrator.appendP8Correction` only, via `POST /p8/corrections` behind local dashboard access; `provenance.source` must be `EXPLICIT_USER_CORRECTION` | Reconstruction | Owner-specific correction command recorded as journal AMENDMENT + control receipt (A10.2); the correction store remains the state owner | A10.2 adds the control receipt around the **same** writer. Moving storage needs shadow read equivalence and an atomic switch | **Keep** owner; add lineage in v0.1.3 |
| Relationship context (`RELATIONSHIP_CONTEXT` interpretation `relationship.current`) | None stored. Derived per turn from long-term Memory evidence plus corrections | No persistent writer | Reconstruction → RELATIONSHIP_CONTEXT | Explicit measured relationship projections (post-v0.1.3 C-series), if E2/E3 justify them | A measured projection passes E2/E3, runs in shadow against the P8 projection, is compared, then the reader switches atomically | **Move** reader to measured projections, then **delete** the P8 derivation. Never store a scalar |
| Other interpretation domains (`BACKGROUND`, `COMMUNICATION_PREFERENCE`, `SHARED_HISTORY`) | Derived per turn from Memory evidence | No persistent writer | Reconstruction | Memory claims with committed lineage (A10.1); preferences stay attributed claims | Lineage-bound Memory claims exist and the projection reads them with provenance | **Keep** as a projection over lineage-bound claims |
| Evidence authority class / support (`EXPLICIT_USER_ORIGINATED` … `ASSISTANT_MODEL_GENERATED`; `DIRECT`/`LIMITED`/`NON_AUTHORITATIVE`) | Mapping from Memory claim `provenanceClass` / `assertion.source` (`adapter.ts memorySourceClass`) | None; pure mapping | Reconstruction, identity evidence | Journal/claim provenance (source, assertor, subject, binding version) | A10.1 guarantees upstream `originRole`/provenance are authority-assigned; the mapping then consumes journal provenance | **Keep** mapping. **Fix upstream input** (see note) |
| Surface-form identity resolution (`P8IdentitySurfaceForm`, mention tiers `TRUSTED_EXPLICIT`…`CANDIDATE_ONLY`) | Pure library API | None | **Library-only**: no production caller | Linguistic projection only; never an account/principal binding | Only after governed principal→Person bindings exist (A10.2/A12.2) may a projection use them as context | **Keep** as linguistic projection. `TRUSTED_EXPLICIT`/`SELF_REPORT` must never be reused as transport authentication |
| Voice person resolution (`resolveP8VoicePerson`, statuses, `voicePersonClaimAssertor`) | Explicit controller voice binding plus local acoustic match | Binding: `bindVoiceProfileToPerson` via `/voice-profiles/:id/person` (local dashboard); resolution is pure | `runtime-speech-identity.ts` → STT attribution and speaker view | Governed identity owner for bindings; P8 keeps the pure resolver | A10.2 journals binding changes with version/revocation; resolver reads versioned bindings | **Keep** resolver; binding authority stays with the identity owner |
| Person records (`id`, `displayName`, `personaId`, `notes`) | Controller product store (not P8) | Product routes; `profile-evidence.ts` also writes a Memory claim directly | Memory scope, P8 address | Same governed identity owner | A10.2 routes the Memory side effect through Runtime control admission | Not a P8 field; listed to prevent a second Person store |
| Epistemic status (`KNOWN`/`UNKNOWN`/`CONFLICTING`/`PARTIAL`/`EMPTY`/`UNAVAILABLE`/`ERROR`) and projection/reconstruction versions | Computed | None | Character ABI projection | Unchanged; later projections must carry equivalent explicit states | — | **Keep** |

**Note: the indirect contamination route.** P8's evidence mapping trusts upstream Memory provenance. Rows produced by the legacy LLM extractor defect carry `originRole: user` and therefore enter P8 as `EXPLICIT_USER_ORIGINATED`/`LIMITED`. The fix belongs upstream in A10.1, not in P8. P8 tests added by A10.1 must show that such a row, once quarantined, no longer reaches the relationship projection.

## Migration rules

1. **One active writer per field.** For each field above, the current writer is the only writer until a cutover. There are no long-lived dual writes.
2. **Shadow → compare → atomic switch → remove.** The replacement computes in shadow from the same committed inputs. A comparison report (differences, reasons, affected scopes) is reviewed. The reader or writer switches in one versioned change, and the old writer or derivation is then deleted. Rollback restores the previous owner/version as a whole.
3. **No new P8 fields.** New semantics (prospective items, measured projections, selection state) get their own owners. They are not added to P8 “temporarily”.
4. **Replacement first.** No P8 field is deleted before its replacement authority is implemented, tested and has passed the relevant gate ([post-v0.1.3 roadmap](post-v0.1.3-roadmap.md), atom **PM1**).
5. **Corrections keep working.** During any migration, an explicit user correction must still retract or revise the affected meaning within one turn.
