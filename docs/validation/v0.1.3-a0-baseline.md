# v0.1.3 A0 — Post-release authority baseline

Audit date: 2026-09-22. Scope: [platform-completion A0](../future/09-v0.1.3-platform-completion.md), using current source/tests over historical status claims.

## Checkout and release identity

- Freshly fetched `origin/main`: `4278eb36b1a01acde82dde27b806dca996739972`.
- New worktree: `/home/ruichen/Projects/YUVI-v0.1.3-platform-20260922`.
- New branch: `codex/v0.1.3-platform-completion-20260922`, created directly from that remote commit with no inherited changes.
- Existing local `main` remains at `020f09f`; it was not advanced or used as the base. No old branch was merged, cherry-picked, reset, cleaned, or reused.
- `v0.1.2` resolves to `722facbbbe08fc56e19477ad8104837759d7b626`, an ancestor of the fetched base. The [published release](https://github.com/Ruichen-0079/YUVI/releases/tag/v0.1.2), dated September 12, has Linux installer/Portable archives, `artifacts.json`, `release-sbom.json`, and `SHA256SUMS`.
- Downloaded manifest and SBOM both identify that tag SHA. Manifest sizes/digests agree with release metadata and checksums; the downloaded SBOM digest is `3afb8b44e96387c2bb81e049a22d28d355b8f3a251c4b17b881780c318960835`. Its 1,918-file inventory declares glibc 2.36. Archive bytes and live artifact startup were not revalidated in A0.

The older artifact-repair document's `c47d670`/“no tag” observation describes an earlier artifact. It does not override the current published receipts. The current release assets also do **not** include every subsequent main fix.

## Leftover classification

| Leftover | Classification | Evidence and disposition |
| --- | --- | --- |
| Open [#315 Portable startup lifecycle](https://github.com/Ruichen-0079/YUVI/pull/315), head `2656117` | **1 — already superseded** for the startup defect | One unique commit, 38 commits behind main, conflicting merge state. `549d50d`, `0fa7d3f`, and `ea10299` landed via #317 and are already in the release tag. Current launcher waits only for its authenticated Supervisor control plane; Desktop sequences bootstrap and Supervisor owns readiness. Current `waitDesktopBinding` bounds attach waiting at 175 seconds without adopting Installed Runtime. Portable config guards, explicit private PostgreSQL start, Mem0 actions, and external STT/TTS ownership have their own current tests. Do not merge the stale patch. |
| #315's additional attach phase DTO/state and unbounded phase polling | **4 — obsolete as a required release repair** | Current binding projection plus bounded UI wait covers the original functional gap. An earlier explicit terminal-error display could be evaluated as a new diagnostic improvement if reproduced need warrants it; absence of the old DTO alone is not a regression. No second bootstrap authority is needed. |
| Published tag's legacy CharacterStyle prompt, speech conservation/playback/lip-sync defects, overlay/history/diagnostic defects | **2 — real v0.1.2 defects, already repaired on current main** | [#316](https://github.com/Ruichen-0079/YUVI/pull/316) retires the competing prompt authority; it is on the merge's main parent, not the release tag's branch. [#318](https://github.com/Ruichen-0079/YUVI/pull/318), [#319](https://github.com/Ruichen-0079/YUVI/pull/319), and [#320](https://github.com/Ruichen-0079/YUVI/pull/320) fix the remaining listed defects after the tag. Any release backport/rebuild belongs to a separate hotfix/release action; do not call the current assets fixed or add these fixes again as v0.1.3 architecture. |
| Platform/context/capability roadmap work | **3 — valid future work needing explicit current-source rebase** | Production already reaches the Phase-6 capability-aware read-text path. Its initial decision is COMPLETE or REQUEST_CAPABILITY, followed by at most one admitted read and one assisted completion. A1's actual gap is a canonical continuation-capable round contract and explicit Runtime-owned bounded state, not another executor/router. Windows/macOS CI and parity remain A10–A12 work. |
| Open [#321 persistent functional self](https://github.com/Ruichen-0079/YUVI/pull/321) and merged north-star document | **3 — separate future research, requiring its own rebase/integration decision** | #321 is documentation-only and explicitly disclaims production authority. Neither it nor `4278eb3` authorizes persistent-self, Life-Layer, Temporal/Continuity, training, or replacement P8 semantics in v0.1.3. |
| Open [#51 AgentBus transport mailbox](https://github.com/Ruichen-0079/YUVI/issues/51) | **4 — obsolete as a v0.1.3 release-work input** | The only issue returned by the all-state issue query is unrelated transport coordination, not a reported product regression. This does not declare the mailbox itself safe to delete. |
| Old roadmap “P8 next”, Character adapter deferred, mandatory Temporal/Continuity ordering, old release workspaces | **4 — obsolete starting assumptions** | Current production callers and `future/production-reachability.md` supersede these older architecture/Phase-6 closure statements. Local workspace existence is not a missing feature or merge obligation. |

PRs/issues were inspected read-only; none were closed or changed. No unresolved **current-main** release blocker was found in the audited scope. The tag-to-main release gap remains explicit and separate from A1.

## Local workspace audit

The correct repository remote is `https://github.com/Ruichen-0079/YUVI.git`. A separate legacy lowercase `yuvi` checkout still names `UV-main`; it is not the campaign workspace.

The YUVI common repository reported 40 worktree records after creating this one: 23 clean, 7 dirty, and 10 missing records (including a duplicate artifact-repair registration, hence 9 distinct missing paths). Dirty worktrees are the installed release checkout, `UV-main-atom18-kde`, `YUVI-v0.1.1`, `YUVI-v0.1.2-portable-build`, `YUVI-v012-finalize`, `yuvi-linux-packaged-local-stt`, and `/mnt/Data/AI/yuvi/UV-main-atom19-slice1`. They contain tracked edits and/or local build/acceptance artifacts. Status was inspected with optional Git locks disabled. All were left untouched; missing records were not pruned. Branch inventory and recent merges were inspected, but no unrelated local work was imported.

## Current authorities and CI

Read `future/README.md`, the v0.1.3 plan, `architecture.md`, Phase-6 closure, production reachability, the v0.1.2 source/artifact audits, and productization validation. Preserve Runtime lifecycle/admission/cancellation/persistence, Memory evidence, P8 interpretation, Character/Harness semantics, sole Cognition normalization, provider registry, DesktopSupervisor, and Presentation ownership. Historical closure status is narrower than current production reachability.

At the exact fetched SHA, hosted [Check](https://github.com/Ruichen-0079/YUVI/actions/runs/35368926822) and [Linux Persistence](https://github.com/Ruichen-0079/YUVI/actions/runs/35368926861) both passed. Check runs Linux Rust library tests, TypeScript/host checks, build, tests, and smoke. Linux Persistence runs pgvector-backed migrations, Memory/Core/Server tests, repeated migrations, and PostgreSQL smoke. These are the only current workflow files; neither proves Windows/macOS build parity or a new packaged release. Recent #317–#320 main runs also passed.

## Local verification and A1 handoff

Installed locked dependencies in the new worktree with `pnpm install --frozen-lockfile` (exit 0; an optional pnpm update lookup reported a network warning).

- 112 tests across 8 files: Supervisor ownership/bootstrap, Web binding/parser, production Character path, Runtime one-shot/admission, Cognition protocol, and current read-text round-trip.
- 64 tests across 7 files: bounded binding wait, speech conservation/lip-sync, durable history route/reconciliation, Companion lock, and prompt authority.
- 17 Node packaging tests: Portable configuration, durable Memory boot, Linux daily package, and pinned static-server routing.
- Release receipt identity/checksum checks and `git diff --check` passed.

No code changed in A0. Local tests do not claim a fresh native GUI, live private-model, PostgreSQL, Windows/macOS, or archive launch run; hosted persistence evidence is identified above.

**A1 is unblocked.** Keep the one-capability production behavior and current 6W wire rejection of unsupported continuation intact while defining the new contract. Reasoning attempts and capability invocations need distinct bounds; cancellation/deadline/execution identity and terminal state belong to Runtime, outside model-facing semantics. A2 alone will activate the bounded multi-round execution loop after a fresh audit. This documentation commit may remain immediately below A1 as the explicitly requested campaign baseline; it carries no unmerged implementation dependency.
